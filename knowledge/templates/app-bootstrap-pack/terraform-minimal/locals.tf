locals {
  app            = "{{APP}}"
  role           = "{{APP}}"
  service        = "{{SERVICE}}"
  cluster        = "{{ENV}}"
  environment    = "{{ENV}}"
  team           = "{{SERVICE}}"
  costing_family = "{{APP}}"
}
