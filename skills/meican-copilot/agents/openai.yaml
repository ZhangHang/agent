version: 1
interface:
  display_name: Meican Copilot Router
  short_description: Route to knowledge docs first, then call focused micro skills.
  default_prompt: Start from knowledge/index.md and answer from domain docs. Only use incident-context, service-trace, or infra-context for execution.
