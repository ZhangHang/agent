---
title: 3.0 美餐码作废
date: 2026-02-04
tags: [project, work]
status: in-progress
---

# 3.0 美餐码作废

## 摘要

昨日（2026-02-03）会议确认：到店/食堂所有都会调用「下单校验」接口，因此直接在 `id-card` 内实现美餐码作废，不在老系统（fan）中实现。

## 范围与职责

- 客户端在部分场景会请求美餐码作废，并上报码
- `payment-adapter` 提供对外 HTTP 作废接口，底层调用 `id-card`
- `id-card` 提供 internal 接口：参数为 `user_id` 与 `code`，作废美餐码并写入 Redis
- `id-card` 查询电子美餐卡身份时先查 Redis，若命中作废则返回新增的“作废码”
- `ops` 增加 `id-card` 新错误码处理与文案（文案与产品确认）

## 调用时序图

```mermaid
sequenceDiagram
    participant Client as 客户端
    participant PA as payment-adapter
    participant IC as id-card
    participant R as Redis

    Client->>PA: 作废请求(码)
    PA->>IC: 内部作废(user_id, code)
    IC->>R: 写入作废记录
    R-->>IC: ok
    IC-->>PA: 作废结果
    PA-->>Client: 作废结果

    Client->>IC: 查询电子美餐卡身份
    IC->>R: 查询是否作废
    R-->>IC: 命中/未命中
    IC-->>Client: 命中则返回新增“作废码”
```

## 人员分工

- 研发：李健（2026-02-04 中午交接）
- 产品：梁伟

## 记录

- 2026-02-04：新增项目记录与范围说明。
